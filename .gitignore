# Rust build output
target/
**/*.rs.bk

# Build / packaging artifacts
build/
dist/
.cache/
*.log
.env

# Editor / OS cruft
.idea/
.vscode/
*.swp
.DS_Store

# Nix: lockfile is generated by `nix flake lock` / first `nix develop`
# (the dev host has no Nix toolchain). Commit it once available; docs/nix.md.
flake.lock